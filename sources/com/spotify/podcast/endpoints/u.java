package com.spotify.podcast.endpoints;

import com.spotify.cosmos.router.Response;
import io.reactivex.s;
import io.reactivex.z;

public interface u {
    s<Response> a(Response response);

    z<Response> b(Response response);
}
