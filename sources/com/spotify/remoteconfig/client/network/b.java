package com.spotify.remoteconfig.client.network;

import com.spotify.remoteconfig.client.model.resolve.FetchType;
import io.reactivex.a;

public interface b {
    a a(FetchType fetchType, g0e g0e);

    a b(FetchType fetchType);
}
