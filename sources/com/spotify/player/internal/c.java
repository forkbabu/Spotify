package com.spotify.player.internal;

import com.spotify.player.model.command.Command;
import io.reactivex.z;

public interface c {
    z<zwd> a(String str, Command command);

    z<zwd> b(String str, Command command);
}
