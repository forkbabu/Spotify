package com.spotify.music.podcast.freetierlikes.tabs.followed;

import android.view.View;
import com.spotify.playlist.models.Show;

public interface v {
    void a(Show show, View view, boolean z);
}
