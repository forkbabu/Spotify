package com.spotify.mobius.android;

import androidx.lifecycle.n;
import androidx.lifecycle.v;

public interface f<T> {
    void a(n nVar, v<T> vVar, v<Iterable<T>> vVar2);

    void b(n nVar, v<T> vVar);
}
