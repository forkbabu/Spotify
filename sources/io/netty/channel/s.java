package io.netty.channel;

import java.net.SocketAddress;

public interface s {
    g close();

    g h0(SocketAddress socketAddress, v vVar);

    g i(v vVar);

    g k(v vVar);

    v l();

    g n(Object obj);

    v q();

    g u(SocketAddress socketAddress, SocketAddress socketAddress2, v vVar);

    g x(Object obj, v vVar);

    g z(Object obj);
}
