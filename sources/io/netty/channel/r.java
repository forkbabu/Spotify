package io.netty.channel;

import java.net.SocketAddress;

public interface r extends i {
    void J(k kVar, SocketAddress socketAddress, SocketAddress socketAddress2, v vVar);

    void L(k kVar);

    void Q(k kVar, v vVar);

    void U(k kVar, v vVar);

    void W(k kVar, Object obj, v vVar);

    void g(k kVar);
}
